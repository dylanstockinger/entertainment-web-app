import { SearchBar } from "../components/SearchBar";

export const Components = () => {
  return (
    <div className="w-full h-screen bg-[#10141E]">
      <SearchBar placeholder="Search for movies or TV series" />
    </div>
  );
};
